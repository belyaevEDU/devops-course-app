---
# argument spec file for belyaev.kube_init.kubeadm-install

argument_specs:
  main:
    short_description: Role description.
    options:
      kubeadm-install_my_variable:
        type: str
        description: A simple string argument for demonstration.
        default: "default_value"
