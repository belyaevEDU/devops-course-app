---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: ubuntu:24.04
    pre_build_image: false
    privileged: true
provisioner:
  name: ansible
  playbooks:
    prepare: prepare.yml
verifier:
  name: testinfra
