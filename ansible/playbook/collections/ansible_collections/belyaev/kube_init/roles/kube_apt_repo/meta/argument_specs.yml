---
# argument spec file for belyaev.kube_init.kube-apt-repo

argument_specs:
  main:
    short_description: Role description.
    options:
      kube-apt-repo_my_variable:
        type: str
        description: A simple string argument for demonstration.
        default: "default_value"
