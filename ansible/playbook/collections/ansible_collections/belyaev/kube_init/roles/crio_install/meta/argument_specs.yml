---
# argument spec file for belyaev.kube_init.cri-o-install

argument_specs:
  main:
    short_description: Role description.
    options:
      cri-o-install_my_variable:
        type: str
        description: A simple string argument for demonstration.
        default: "default_value"
