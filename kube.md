# kubernetes docs
v1.34.7

⚠️ outdated and missing lots of info ⚠️

hosts:

```
# Kubernetes The Hard Way
10.184.0.33 server.kubernetes.local server
10.184.0.133 worker1.kubernetes.local worker1
10.184.0.150 worker2.kubernetes.local worker2
```

kubectl is configured on `server` on root & master users via ~/.bashrc modification, config is ~/admin.kubeconfig